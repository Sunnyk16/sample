import  ReactDOM  from 'react-dom/client';
import './index.css'

const root = ReactDOM.createRoot(document.getElementById('root'));

function cards(){
    return(
        <div 
    )
}

root.render(
  <div className='boxes'>
  
  </div>
)